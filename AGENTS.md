# AGENTS.md — Cloud Honeypot Client

Before changing API/agent behavior, follow **honeypot-contract**:

1. `../honeypot-contract/VERSION` + `INDEX.md`
2. Relevant `api/*` or `agent/*`
3. Contract first on behavior changes; then code

Cursor rule: `.cursor/rules/honeypot-contract.mdc`  
Pointer: `contract/README.md`
